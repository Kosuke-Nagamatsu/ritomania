<% if @current_page == 'posts' %>
  $("#favorite-btn-<%= @post.id %>").html("<%= j(render partial: 'favorites/favorite', locals: {post: @post }) %>");
<% else %>
  $('#favorite-btn-<%= @post.id %>').remove();
<% end %>
