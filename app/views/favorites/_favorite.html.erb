<% if post.favorited_by?(current_user) %>
  <li><%= link_to post.favorites.count, post_favorite_path(post.id, post.favorite_ids), method: :delete, remote: true %></li>
<% else %>
  <li><%= link_to post.favorites.count, post_favorites_path(post.id), method: :post, remote: true %></li>
<% end %>
