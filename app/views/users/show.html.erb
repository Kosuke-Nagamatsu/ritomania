<h1 class="bg-info text-center main-header"><%= "#{@user.name}さんのプロフィール" %></h1>
<div class="container flexbox-container-vertical-center">
  <div>
    <ul class="list-group">
      <li class="list-group-item text-center"><%= image_tag @user.icon.variant(gravity: 'Center', resize: '250x250'), class:'img-circle' if @user.icon.attached? %></li>
      <li class="list-group-item"><%= t('view.email') %>：<%= "#{@user.email}" %></li>
      <% if @user.islands.present? %>
        <li class="list-group-item"><%= t('view.favorite_islands') %>：
          <% @user.islands.each do |island|%>
            <%= link_to island.name, posts_path(island.id, search_island: "true"), class: 'label label-primary' %>
          <% end %>
        </li>
      <% end %>
      <li class="list-group-item"><%= link_to t('view.edit_profile'), edit_user_registration_path %></li>
    </ul>
  </div>
</div>
