<h1 class="bg-info text-center main-header">投稿編集</h1>
<div class="container">
  <%= render 'form', post: @post %>
  <%= link_to @post do %>
    <i class="fa fa-chevron-left "></i>
    <span class="awesome-icon"><%= t('view.back') %></span>
  <% end %>
</div>
